# noahforge

## Folder

`projects/noahforge/`

## Purpose

Website project copied from `D:\noahforge`.

## Main Files And Folders

- `app/`
- `components/`
- `lib/`
- `public/`
- `ui/`
- `avani_pdf_package/`
- `package.json`
- `package-lock.json`
- `.env.local`

## Current State

- Synced through Google Drive, not GitHub.
- `node_modules` and `.next` were intentionally not copied.
- The old empty `projects/NovoForge/` folder was removed from the shared workspace.

## Next Actions

- Use this context when the user says to work on `noahforge`.
- On Mac, run `npm install` in `projects/noahforge` before running the site if dependencies are missing.

## Commands

```bash
npm install
npm run dev
```
